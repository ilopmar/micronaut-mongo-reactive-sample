---
micronaut:
    application:
        name: mongoreactive
                
#Uncomment to set server port
#    server:
#        port: 8080

---
mongodb:
    uri: "mongodb://${MONGO_HOST:localhost}:${MONGO_PORT:27017}"